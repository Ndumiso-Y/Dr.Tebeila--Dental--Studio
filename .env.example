# Supabase Configuration
# ========================
# Get these values from your Supabase project dashboard:
# https://app.supabase.com/project/_/settings/api

# Supabase Project URL
VITE_SUPABASE_URL=https://your-project-id.supabase.co

# Supabase Anon/Public Key (safe to use in frontend)
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Supabase Service Role Key (NEVER expose in frontend - backend only)
# Used for admin operations and bypassing RLS
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Application Configuration
# ==========================

# Environment (development, staging, production)
NODE_ENV=development

# Application URL
VITE_APP_URL=http://localhost:5173

# Feature Flags
# ==========================
VITE_ENABLE_PWA=true
VITE_ENABLE_OFFLINE_MODE=true

# Tenant Configuration (for multi-tenant setup)
# ===============================================
# Note: tenant_id is stored in JWT claims after authentication
# These are for development/testing only

# Development tenant ID (optional - for testing)
# DEV_TENANT_ID=your-tenant-uuid-here

# Brand Colors (from tenant settings)
VITE_PRIMARY_COLOR=#05984B
VITE_SECONDARY_COLOR=#0E8ECC

# Invoice Configuration
# =======================
VITE_INVOICE_PREFIX=DEV
VITE_DEFAULT_CURRENCY=ZAR
VITE_DEFAULT_VAT_RATE=15

# PDF Generation (for server-side rendering)
# ============================================
# Puppeteer configuration for server-side PDF generation
PUPPETEER_EXECUTABLE_PATH=/usr/bin/chromium-browser

# Email Configuration (optional - for notifications)
# ====================================================
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=noreply@drtebeila.co.za
# SMTP_PASSWORD=your-password
# SMTP_FROM=Dr.Tebeila Dental Studio <noreply@drtebeila.co.za>

# Analytics (optional)
# =====================
# VITE_GA_TRACKING_ID=G-XXXXXXXXXX

# Sentry Error Tracking (optional)
# ==================================
# VITE_SENTRY_DSN=https://xxx@xxx.ingest.sentry.io/xxx
# SENTRY_AUTH_TOKEN=your-sentry-auth-token

# Notes:
# ======
# 1. Copy this file to .env.local for local development
# 2. Never commit .env.local to git (it's in .gitignore)
# 3. Update production values in your deployment platform (Vercel, Netlify, etc.)
# 4. VITE_ prefix makes variables available in frontend via import.meta.env
# 5. Variables without VITE_ prefix are backend-only (Node.js/Edge Functions)
